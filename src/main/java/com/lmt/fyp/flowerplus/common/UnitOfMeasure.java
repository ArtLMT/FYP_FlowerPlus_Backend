package com.lmt.fyp.flowerplus.common;

public enum UnitOfMeasure {
    STEM,    // roses, lilies, tulips — the default for flowers
    PIECE,   // vases, cards, bows, ribbon spools as whole items
    METRE,   // ribbon cut to length
    SHEET  // wrapping paper, cellophane
}
