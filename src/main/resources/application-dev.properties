# dev: the local app and every test run (ADR 0003).
# Tests delete all users before each test, so accounts created while running
# the app locally do not survive a test run.
application.database.schema=test
